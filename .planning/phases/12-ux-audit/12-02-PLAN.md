---
phase: 12-ux-audit
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/components/navigation.tsx
  - src/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Navigation uses Phosphor icons instead of Lucide icons"
    - "Navigation uses semantic colour tokens — works correctly in both light and dark modes"
    - "Theme toggle is visible in the navigation bar"
    - "Sign out uses shadcn Button component instead of raw <a> tag"
    - "Landing page uses semantic colours and works in dark mode"
    - "Navigation is usable on mobile with icon-only display"
  artifacts:
    - path: "src/components/navigation.tsx"
      provides: "Navigation bar with Phosphor icons, semantic colours, theme toggle, mobile-responsive"
      contains: "@phosphor-icons/react"
    - path: "src/app/page.tsx"
      provides: "Landing page with semantic colours and dark mode support"
  key_links:
    - from: "src/components/navigation.tsx"
      to: "src/components/theme-toggle.tsx"
      via: "Navigation renders ThemeToggle component"
      pattern: "ThemeToggle"
---

<objective>
Overhaul the navigation bar and landing page to use Phosphor icons, semantic colour tokens, and include the theme toggle. These are the first things users see and set the visual tone for the entire app.

Purpose: Navigation appears on every authenticated page — fixing it first ensures consistent baseline for all subsequent page work. Landing page is the entry point and must establish the design language.

Output: Linear-inspired navigation with Phosphor icons, theme toggle, semantic colours, and a polished landing page.
</objective>

<execution_context>
@/Users/anthonygeorge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonygeorge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-ux-audit/12-RESEARCH.md
@.planning/phases/12-ux-audit/12-01-SUMMARY.md
@src/components/navigation.tsx
@src/app/page.tsx
@src/components/theme-toggle.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overhaul navigation bar</name>
  <files>src/components/navigation.tsx</files>
  <action>
    Rewrite `src/components/navigation.tsx` to use Phosphor icons, semantic colours, theme toggle, and improved structure:

    1. Replace all Lucide icon imports with Phosphor equivalents:
       - `LayoutDashboard` -> `SquaresFour` from `@phosphor-icons/react`
       - `BarChart3` -> `ChartBar`
       - `Video` -> `VideoCamera`
       - `Brain` -> `Brain`
       - `ClipboardCheck` -> `ClipboardText`
       - `Shield` -> `Shield`
       - `RefreshCw` -> `ArrowsClockwise`
       - `LogOut` -> `SignOut`

    2. Replace all hardcoded colours with semantic tokens:
       - `bg-white` -> `bg-card`
       - `bg-blue-50 text-blue-700` (active) -> `bg-accent text-accent-foreground` (or `bg-primary/10 text-primary`)
       - `text-gray-600` -> `text-muted-foreground`
       - `hover:bg-gray-50` -> `hover:bg-accent`
       - `hover:text-gray-900` -> `hover:text-accent-foreground`
       - `from-blue-600 to-purple-600` (logo gradient) -> keep gradient but use `from-primary to-primary/70` or keep as brand colour (gradients are brand, acceptable to keep)

    3. Replace sign out raw `<a>` with `<Button variant="ghost">` wrapping a Link or using onClick with signOut from next-auth.

    4. Import and render `<ThemeToggle />` from `@/components/theme-toggle` in the right section of the navbar, between nav links and sign out.

    5. Apply Linear-inspired styling:
       - Nav bar: `border-b border-border bg-card/80 backdrop-blur-sm` (subtle glass effect)
       - Container: `mx-auto max-w-7xl px-6` (consistent with page layout pattern)
       - Active link: subtle background with `font-medium`
       - Inactive links: `text-muted-foreground hover:text-foreground`
       - Icons: size 18 (slightly smaller than 20 for nav compactness)
       - Height: keep `h-14` (56px) -- slightly more compact than current h-16

    6. Mobile improvements:
       - Icons visible on all screen sizes
       - Labels hidden on small screens (`hidden md:block`)
       - Nav items use `gap-0.5` for tighter spacing on mobile
       - Consider tooltip for icon-only nav items (use title attribute as simple tooltip)
  </action>
  <verify>
    Run `npm run build` to verify no import errors. Verify no Lucide imports remain in navigation.tsx. Verify no hardcoded colour classes (bg-white, text-gray-*, bg-blue-*) remain.
  </verify>
  <done>
    Navigation uses Phosphor icons, semantic colour tokens (bg-card, text-muted-foreground, bg-accent), theme toggle visible, sign out uses Button component, mobile-responsive with icon-only display on small screens. Zero hardcoded colours.
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish landing page</name>
  <files>src/app/page.tsx</files>
  <action>
    Update `src/app/page.tsx` to use semantic colours and Linear-inspired design:

    1. Read the current landing page and identify all hardcoded colours.

    2. Replace all hardcoded colours with semantic tokens:
       - All `bg-gray-*`, `text-gray-*` -> semantic equivalents
       - All `bg-blue-*`, `text-blue-*` -> `bg-primary`, `text-primary`, `bg-primary/10`
       - All `bg-white` -> `bg-card` or `bg-background`
       - Keep the brand gradient on the logo/hero if it exists

    3. Standardise the page layout:
       - Background: `bg-background` (not `bg-gray-50`)
       - Text: `text-foreground` for headings, `text-muted-foreground` for descriptions
       - Cards/sections: `bg-card` with `border border-border`
       - Sign-in button: use shadcn `<Button>` component if not already

    4. Ensure the page works in dark mode:
       - No hardcoded colours remain
       - Sufficient contrast in both themes
       - Test: visually imagine the page with dark background — all text should be readable

    5. Typography: use `text-3xl font-semibold tracking-tight` for main heading (landing page gets larger title than inner pages), `text-lg text-muted-foreground` for subtitle.

    6. Spacing: generous whitespace, `py-16` or more for hero section, `space-y-8` between sections.
  </action>
  <verify>
    Run `npm run build`. Grep for hardcoded colours (bg-gray, text-gray, bg-white, bg-blue) in src/app/page.tsx — should find zero.
  </verify>
  <done>
    Landing page uses only semantic colour tokens. Works correctly in both light and dark modes. Typography follows standardised scale. Spacious Linear-inspired layout. Sign-in uses Button component.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Zero Lucide imports in navigation.tsx
3. Zero hardcoded colour classes in navigation.tsx and page.tsx
4. ThemeToggle rendered in navigation
5. Sign out uses Button component, not raw <a>
6. Navigation has backdrop-blur and semantic colours
</verification>

<success_criteria>
Navigation and landing page fully migrated to Phosphor icons and semantic colours. Theme toggle visible and functional. Both pages render correctly in light and dark modes with Linear-inspired aesthetic.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ux-audit/12-02-SUMMARY.md`
</output>
