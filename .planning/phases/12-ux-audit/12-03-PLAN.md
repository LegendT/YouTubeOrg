---
phase: 12-ux-audit
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/app/dashboard/page.tsx
  - src/components/playlist-list.tsx
  - src/components/quota-display.tsx
  - src/components/sync-button.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard page uses semantic colour tokens — works in both light and dark modes"
    - "Dashboard page uses standardised layout: max-w-7xl, text-2xl font-semibold title, text-sm text-muted-foreground description"
    - "Playlist list uses semantic colours for card backgrounds, text, and borders"
    - "Quota display uses semantic colours"
    - "Sync button uses shadcn Button component with semantic colours"
    - "Empty state on dashboard uses EmptyState component when no playlists exist"
  artifacts:
    - path: "src/app/dashboard/page.tsx"
      provides: "Dashboard page with standardised layout and semantic colours"
    - path: "src/components/playlist-list.tsx"
      provides: "Playlist list with semantic colours and dark mode support"
    - path: "src/components/quota-display.tsx"
      provides: "Quota display with semantic colours"
    - path: "src/components/sync-button.tsx"
      provides: "Sync button with Phosphor icon and semantic colours"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/components/ui/empty-state.tsx"
      via: "Dashboard uses EmptyState when no playlists"
      pattern: "EmptyState"
---

<objective>
Migrate the dashboard page and its child components (playlist list, quota display, sync button) to semantic colours, standardised layout, and Phosphor icons.

Purpose: Dashboard is the primary landing page after login. It must establish the visual standard that all other pages follow.

Output: Dashboard with consistent layout, semantic colours, Phosphor icons, proper empty state, and dark mode support.
</objective>

<execution_context>
@/Users/anthonygeorge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonygeorge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-ux-audit/12-RESEARCH.md
@.planning/phases/12-ux-audit/12-01-SUMMARY.md
@src/app/dashboard/page.tsx
@src/components/playlist-list.tsx
@src/components/quota-display.tsx
@src/components/sync-button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardise dashboard page layout and colours</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
    Read and update `src/app/dashboard/page.tsx`:

    1. Replace the page container/layout to match the standardised pattern from RESEARCH.md:
       - Outer: `<main className="min-h-[calc(100vh-3.5rem)] bg-background">`
       - Inner: `<div className="mx-auto max-w-7xl px-6 py-8 space-y-8">`
       - Replace current `max-w-6xl mx-auto` with `max-w-7xl`
       - Replace `bg-gray-50` with `bg-background`

    2. Standardise the page header:
       - Title: `text-2xl font-semibold tracking-tight text-foreground` (replace `text-3xl font-bold`)
       - Description: `text-sm text-muted-foreground` (replace `text-gray-600`)
       - Action button (if any) positioned top-right

    3. Replace all hardcoded colours:
       - `bg-gray-50` -> `bg-background`
       - `bg-white` -> `bg-card`
       - `text-gray-900` -> `text-foreground`
       - `text-gray-600` -> `text-muted-foreground`
       - `text-gray-500` -> `text-muted-foreground`
       - `border-gray-200` -> `border-border`
       - Any blue/green/red hardcoded colours -> semantic equivalents

    4. Replace any Lucide icon imports with Phosphor equivalents.

    5. If the dashboard has an empty state (no playlists synced), replace it with the `<EmptyState />` component from `@/components/ui/empty-state`:
       - Icon: use an appropriate Phosphor icon (e.g., `ListDashes` or `MusicNotesPlus`)
       - Title: "No playlists synced"
       - Description: "Sync your YouTube playlists to get started organising your video library."
       - Action: `{ label: "Sync Playlists", href: "/sync" }` or an onClick to trigger sync

    6. Card components: if using raw divs with card-like styling, replace with shadcn `<Card>` / `<CardHeader>` / `<CardContent>` or at minimum ensure they use `bg-card border border-border rounded-lg`.
  </action>
  <verify>
    Run `npm run build`. Grep for hardcoded colours in dashboard/page.tsx. Verify page uses max-w-7xl container.
  </verify>
  <done>
    Dashboard page uses standardised layout (max-w-7xl, text-2xl font-semibold heading), all semantic colours, no hardcoded grays/whites/blues. Empty state uses EmptyState component.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate playlist-list, quota-display, and sync-button components</name>
  <files>
    src/components/playlist-list.tsx
    src/components/quota-display.tsx
    src/components/sync-button.tsx
  </files>
  <action>
    For each of these three components:

    **playlist-list.tsx:**
    1. Read the file and identify all hardcoded colours.
    2. Replace hardcoded colours with semantic tokens using the RESEARCH.md mapping.
    3. Replace any Lucide imports with Phosphor equivalents.
    4. Ensure list items use `bg-card`, `text-foreground`, `text-muted-foreground`, `border-border`.
    5. Ensure hover states use semantic colours (`hover:bg-accent`).

    **quota-display.tsx:**
    1. Read the file and identify all hardcoded colours.
    2. Replace hardcoded colours with semantic tokens.
    3. Replace any Lucide imports with Phosphor equivalents.
    4. For quota progress indicators:
       - Green states -> use `text-success` / `bg-success/10` (using the new success token)
       - Amber/warning states -> use `text-warning` / `bg-warning/10`
       - Red/danger states -> use `text-destructive` / `bg-destructive/10`

    **sync-button.tsx:**
    1. Read the file and identify all hardcoded colours.
    2. Replace hardcoded colours with semantic tokens.
    3. Replace Lucide `RefreshCw` with Phosphor `ArrowsClockwise`.
    4. If using raw `<button>`, switch to shadcn `<Button>`.
    5. Replace any `Loader2` spinner with `<Spinner />` from `@/components/ui/spinner`.
  </action>
  <verify>
    Run `npm run build`. Grep all three files for hardcoded colours (bg-gray, text-gray, bg-white, bg-blue, bg-green, bg-red, text-red, text-green). All should return zero matches.
  </verify>
  <done>
    All three components use semantic colours, Phosphor icons, and shadcn Button where applicable. No hardcoded colours remain. Loading states use Spinner component.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Zero hardcoded colour classes in all 4 files
3. Zero Lucide imports in all 4 files
4. Dashboard uses standardised max-w-7xl layout
5. Title is text-2xl font-semibold (not text-3xl font-bold)
6. Empty state uses EmptyState component
</verification>

<success_criteria>
Dashboard page and all child components fully migrated to semantic colours and Phosphor icons. Page layout matches the standardised pattern. Works correctly in both light and dark modes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ux-audit/12-03-SUMMARY.md`
</output>
