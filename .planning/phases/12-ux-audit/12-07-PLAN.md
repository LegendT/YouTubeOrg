---
phase: 12-ux-audit
plan: 07
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/app/videos/page.tsx
  - src/components/videos/video-browse-page.tsx
  - src/components/videos/video-card.tsx
  - src/components/videos/video-grid.tsx
  - src/components/videos/video-toolbar.tsx
  - src/components/videos/category-sidebar.tsx
  - src/components/videos/move-copy-dialog.tsx
autonomous: true

must_haves:
  truths:
    - "Videos page uses semantic colours — works in both light and dark modes"
    - "All video components use Phosphor icons instead of Lucide"
    - "Video cards display correctly in dark mode (thumbnails, text, badges)"
    - "Category sidebar uses semantic colours with proper hover/active states"
    - "Video toolbar (search, sort, select) uses semantic colours"
    - "Move/copy dialog uses semantic colours"
    - "Empty state uses EmptyState component when no videos"
  artifacts:
    - path: "src/components/videos/video-browse-page.tsx"
      provides: "Video browse page with semantic colours and Phosphor icons"
      contains: "@phosphor-icons/react"
    - path: "src/components/videos/video-card.tsx"
      provides: "Video card with semantic colours for dark mode"
    - path: "src/components/videos/category-sidebar.tsx"
      provides: "Category sidebar with semantic colours"
  key_links:
    - from: "src/components/videos/video-browse-page.tsx"
      to: "src/components/ui/empty-state.tsx"
      via: "Empty state when no videos in category"
      pattern: "EmptyState"
---

<objective>
Migrate the videos page and all video-related components to semantic colours, Phosphor icons, and proper empty states.

Purpose: The videos page is a full-bleed split-panel layout with sidebar, toolbar, and virtualized grid. It handles 4,000+ videos and must work flawlessly in dark mode.

Output: Complete videos page ecosystem with semantic colours, Phosphor icons, EmptyState, and dark mode support.
</objective>

<execution_context>
@/Users/anthonygeorge/.claude/get-shit-done/workflows/execute-plan.md
@/Users/anthonygeorge/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-ux-audit/12-RESEARCH.md
@.planning/phases/12-ux-audit/12-01-SUMMARY.md
@src/app/videos/page.tsx
@src/components/videos/video-browse-page.tsx
@src/components/videos/video-card.tsx
@src/components/videos/video-grid.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate video-browse-page, video-grid, and video-card</name>
  <files>
    src/app/videos/page.tsx
    src/components/videos/video-browse-page.tsx
    src/components/videos/video-grid.tsx
    src/components/videos/video-card.tsx
  </files>
  <action>
    **videos/page.tsx:**
    1. Replace any hardcoded colours with semantic tokens
    2. Background: `bg-background`

    **video-browse-page.tsx (main orchestrator):**
    1. Replace ALL Lucide icon imports with Phosphor equivalents
    2. Replace ALL hardcoded colours with semantic tokens
    3. Replace any `Loader2` with `<Spinner />`
    4. Add EmptyState component for when no videos exist:
       - Import `EmptyState` from `@/components/ui/empty-state`
       - Icon: `VideoCamera` from Phosphor
       - Title: "No videos found"
       - Description: "Sync your YouTube playlists to browse and organise your videos."
       - Action: `{ label: "Sync Playlists", href: "/sync" }`
    5. Add EmptyState for when category is selected but has no videos:
       - Icon: `FolderOpen` from Phosphor
       - Title: "No videos in this category"
       - Description: "Move or copy videos from other categories to get started."

    **video-grid.tsx (virtualized grid):**
    1. Replace any hardcoded colours (grid container background, empty cell colours)
    2. Empty cells: `bg-transparent` or remove styling
    3. Grid background: `bg-background`

    **video-card.tsx:**
    1. Replace ALL hardcoded colours:
       - Card background: `bg-card`
       - Card border: `border-border`
       - Title text: `text-foreground`
       - Channel name: `text-muted-foreground`
       - Duration overlay: `bg-foreground/80 text-background` (dark overlay on thumbnail)
       - Category badges: keep colourful but ensure contrast in dark mode
       - Checkbox overlay: `bg-card/90` backdrop
    2. Replace any Lucide icons with Phosphor
    3. Hover state: `hover:shadow-md` or `hover:border-border/80` (subtle elevation change)
    4. Selected state: `ring-2 ring-primary`
  </action>
  <verify>
    Run `npm run build`. Grep all 4 files for Lucide imports and hardcoded colours.
  </verify>
  <done>
    Video page, browse page, grid, and card all use semantic colours and Phosphor icons. Empty states use EmptyState component. Video cards render correctly in dark mode.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate category-sidebar, video-toolbar, and move-copy-dialog</name>
  <files>
    src/components/videos/category-sidebar.tsx
    src/components/videos/video-toolbar.tsx
    src/components/videos/move-copy-dialog.tsx
  </files>
  <action>
    **category-sidebar.tsx:**
    1. Replace ALL hardcoded colours with semantic tokens:
       - Sidebar background: `bg-card` or `bg-muted/30`
       - Sidebar border: `border-r border-border`
       - Category items: `text-foreground`
       - Category counts: `text-muted-foreground`
       - Active category: `bg-accent text-accent-foreground`
       - Hover: `hover:bg-accent/50`
       - "All Videos" item: same styling as category items
    2. Replace any Lucide icons with Phosphor (FolderOpen, Folder, etc.)
    3. Scrollbar: ensure it's styled for dark mode (or use ScrollArea component)

    **video-toolbar.tsx:**
    1. Replace ALL hardcoded colours:
       - Toolbar background: `bg-card/95 backdrop-blur-sm` (sticky)
       - Search input: `bg-background border-border text-foreground placeholder:text-muted-foreground`
       - Sort dropdown: semantic colours
       - Select all / batch actions: semantic colours
    2. Replace Lucide icons with Phosphor:
       - `Search` -> `MagnifyingGlass`
       - `X` -> `X`
       - `CheckSquare` -> `CheckSquare`
       - Any sort icons
    3. Replace `Loader2` with `<Spinner />`

    **move-copy-dialog.tsx:**
    1. Replace ALL hardcoded colours with semantic tokens
    2. Replace Lucide icons with Phosphor
    3. Category list in dialog: `bg-card`, `hover:bg-accent`, `text-foreground`
    4. Warning section (bulk operation): `bg-warning/10 text-warning border-warning/20`
    5. Replace `Loader2` with `<Spinner />`
  </action>
  <verify>
    Run `npm run build`. Grep all 3 files for Lucide imports and hardcoded colours.
  </verify>
  <done>
    Category sidebar, video toolbar, and move-copy dialog all use semantic colours and Phosphor icons. Sidebar has proper active/hover states for dark mode. Toolbar search input uses semantic colours.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Zero Lucide imports across all 7 files
3. Zero hardcoded colour classes across all 7 files
4. Empty states use EmptyState component
5. Video cards render with bg-card and text-foreground
6. Category sidebar has semantic active/hover states
</verification>

<success_criteria>
Complete videos page ecosystem migrated to semantic colours and Phosphor icons. Empty states use EmptyState component. All components work correctly in both light and dark modes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ux-audit/12-07-SUMMARY.md`
</output>
